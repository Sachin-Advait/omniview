import 'package:flutter/material.dart';

class Profit extends StatelessWidget {
  const Profit({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Profit Content", style: TextStyle(color: Colors.white)),
    );
  }
}
