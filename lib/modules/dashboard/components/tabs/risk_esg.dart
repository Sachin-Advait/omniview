import 'package:flutter/material.dart';

class RiskEsg extends StatelessWidget {
  const RiskEsg({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Risk and Esg Content",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
